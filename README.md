# ManagementSystemUsingSpringJDBC
A Java-based console Faculty Management System built using the Spring Framework with XML configuration. It supports CRUD operations through a DAO layer and uses custom exceptions for strict input validation (ID, name, email, room, month). TestFaculty.java manages user interaction, while faculty.xml handles bean and database configuration.
